import { Fragment } from "react"

export const AddToBagButton = () => {
    return <>
        <Fragment>
            <button type="submit">
                Add to bag
            </button>
        </Fragment>
    </>
}