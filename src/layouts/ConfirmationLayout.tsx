import {Fragment} from "react";
import {ConfirmationForm} from "@/components/Fragments/ConfirmationForm";

export const ConfirmationLayout = () => {
    return (
        <Fragment>
            <ConfirmationForm />
        </Fragment>
    )
}