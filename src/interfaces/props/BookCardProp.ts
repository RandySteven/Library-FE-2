export interface BookCardProp {
    id: number
    title: string
    image: string
    rating: number
    status: string
}

export interface BookBagProp {
    id: number
    dataKey: number
    title: string
    image: string
}